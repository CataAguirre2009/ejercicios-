actividades

3-mostrar por pantalla las primeras diez filas de las primeras tres columnas

4- mostrar las ultimas 8 filas de la ultima columna

5-abrir el csv, mostrar las columnas gender y maths score. mostrar la cantidad de filas y columnas que tiene. mostrar las primeras 4 filas. mostrar las ultimas 4 filas

6-abrir el csv, mostrar las 100 primeras filas. mostrar la cantidad. mostras la ultima fila

7-abrir el csv. mostrar todas las columnas menos la ultima. mostrar las primeras 7 filas. mostrar la cantidad de filas y columnas. mostrar la cantidad. mostrar las ultimas 2 filas

8-mostrar las columnas gender y maths score. mostrar el tipo de dato de la columna gender. mostrar si hay filas o columnas vacias. mostrar la cantidad de filas y columnas que tiene. mostrar las ultimas 5 filas. 

9-abrir el csv las ultimas 3 columnas. mostrar las 100 primerasa filas. mostrar la cantidad de la primera columna. mostrar si hay filas vacias. mostrar los diferentes tipo de dato de lasa columnas

10-instalar el matplotlib, y hacer un arreglo de eje cartesiano

11-usar el pandas y el matplotlib para graficar las columnas maths score y reading score

12-abrir csv. sumar la columna maths score. encontrar el minimo y el maximo en la misma. contar los elementos de la columna maths score

13- abrir el csv. mostrar las columnas gender y maths score. mostrar las primeras 8 filas. mostrar las ultimas 4 filas. mostrar los tipo de dato de cada columna. mostrar si hay algun espacio sin usar. contar cantidad de cada columna. 

14-abrir el csv. mostrar la columna math score y hight school, ordenar con sort_values la columna math score, ordernar con sort_values la columna math score

15-abrir el user csv. mostrar la columna pais y edad. ordenar la columna edad y mostrar los 5 usuarios mas viejos de alemania.
obtener los 5 usuarios mas viejos de alemania.









